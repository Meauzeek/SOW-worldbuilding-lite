const escapeText=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function formatCharacterLinks(rows=[]){return rows.map(r=>[r.name,r.relation,r.notes,r.source].map(s=>String(s||'').replace(/[\r\n|｜]+/g,' ')).join('｜')).join('\n');}
export function parseCharacterLinks(text=''){const rows=text.split(/\r?\n/).map(s=>s.trim()).filter(Boolean);if(rows.length>100)throw Error('每座城市最多填写100条角色关系');return rows.map(line=>{const [name,relation='',notes='',source='']=line.split(/[|｜]/).map(s=>s.trim());if(!name||name.length>200||relation.length>200||notes.length>2000||source.length>1000)throw Error('角色关系内容过长或缺少姓名');return {name,relation,notes,source};});}
export function characterLinksMarkup(rows=[]){return rows.length?rows.map(r=>`<div class="character-link"><b>${escapeText(r.name)}</b> <span>${escapeText(r.relation||'关联人物')}</span>${r.notes?`<p>${escapeText(r.notes)}</p>`:''}${r.source?`<small>${escapeText(r.source)}</small>`:''}</div>`).join(''):'<p>尚未填写关联角色</p>';}
